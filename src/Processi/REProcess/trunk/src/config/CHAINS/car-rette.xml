<?xml version="1.0"?>
<j:jelly trim="false" xmlns:define="jelly:define" xmlns:j="jelly:core"
	     xmlns:x="jelly:xml" xmlns:re="reTagLib" xmlns:log="jelly:log">
	
	<define:taglib uri="reTagLib">
		<define:jellybean name="bridge" className="it.webred.rulengine.chains.bridge.JellyCommandBridge" />
	</define:taglib>
	
	<!-- 0 -->
	<re:bridge codiceComando="XML-BOLL" context="${ctx}" connessione="DWH" var="cmd0" />
	
	<!-- 1 -->
	<re:bridge codiceComando="XML-BOLL-D" context="${ctx}" connessione="DWH" var="cmd0" />
	
	<!-- 2 -->
	<re:bridge codiceComando="XML-BOLL-R" context="${ctx}" connessione="DWH" var="cmd0" />
	
	<!-- 3 -->
	<re:bridge codiceComando="XML-BOLL-C" context="${ctx}" connessione="DWH" var="cmd0" />
	
	<!-- 4 -->
	<re:bridge codiceComando="XML-BOLL-S" context="${ctx}" connessione="DWH" var="cmd0" />


</j:jelly>